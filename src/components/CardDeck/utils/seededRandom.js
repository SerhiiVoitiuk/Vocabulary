// Детермінований "випадковий" генератор: те саме seed завжди дає той самий результат.
// Потрібен, щоб зорепад на картці не перегенеровувався по-новому при кожному рендері.
export function seededRandom(n) {
  const x = Math.sin(n * 999) * 10000;
  return x - Math.floor(x);
}
